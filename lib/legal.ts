/**
 * Shared constants for the /privacy and /terms pages.
 *
 * These documents are DRAFT starting-point copy, not legal advice — see
 * `LEGAL_DRAFT`. The dates are intentionally unset: a travel-industry / privacy
 * attorney fills the `[BRACKETED]` values (legal entity name, mailing address,
 * phone, effective dates, retention periods, governing-law state, fee structure)
 * and reviews both documents before launch. Flip `LEGAL_DRAFT` to `false` and
 * set the two dates once that review is done.
 */
export const LEGAL_DRAFT = true;

/** Effective / last-updated dates — filled at legal sign-off. */
export const LEGAL_EFFECTIVE_DATE = "[DATE — pending legal review]";
export const LEGAL_LAST_UPDATED = "[DATE — pending legal review]";
